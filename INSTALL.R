
options(repos = c(CRAN = "https://cloud.r-project.org"))

# Install the required packages
install.packages(c("dplyr", "git2r", "logger", "readr", "tidyr", "quanteda", "ymlthis", "git2r", "reticulate"))
