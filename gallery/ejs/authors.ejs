
```{=html}

<% for (const item of items) { %>

<h2><%- item.category %></h2>

<p><%- item.description %></p>

<div class="list grid" style="column-gap: 10px;">

<% for (const tile of item.tiles) { %>
  <div class="card border-2 rounded-3 g-col-12 g-col-sm-6 g-col-md-4 mb-2" <%= metadataAttrs(tile) %>>
    <div class="card-header py-1 px-2 bg-blue">
      <small class="card-text text-nowrap inline-block">
        <a href="<%- tile.href %>" class="listing-title"><%= tile.name %></a>
        <span class="listing-subtitle">(<%= tile.team %>)</span>
      </small>
      
    </div>
    <a href="<%- tile.href %>">
      <img src="<%- tile.thumbnail %>" alt="<%- tile.description %>" class="card-img-top"/>
    </a>
    <small class="card-text text-nowrap inline-block">
      <% if (tile.twitter) { %> 
        <a href="https://twitter.com/<%- tile.twitter %>"><i class="bi bi-twitter"></i></a>
      <% } %>
      <% if (tile.github) { %> 
        <a href="https://github.com/<%- tile.github %>"><i class="bi bi-github"></i></a>
      <% } %>
    </small>
    
  </div>
<% } %>

</div>

<% } %>

```